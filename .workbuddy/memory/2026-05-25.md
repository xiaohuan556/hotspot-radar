# 2026-05-25

## UI 大改版
- 从深色 Grid 布局改为浅色 Tab 切换布局
- 白色/浅灰主题，去掉所有暗色调
- 顶部横向标签栏（6个分类），点击切换下方内容区
- 每条热点增加左侧分类色条
- 翻译按钮交互优化（hover变实心）
- 分类面板独立显示摘要+列表

## v4 大改版 — 双击跳转 + 视觉重塑
- 核心：HotItemWidget 添加 mouseDoubleClickEvent，双击直接打开原文链接
- 整行 hover 变蓝 + 光标变手型，可操作感更强
- 每条热点增加序号（01-20）、来源标签、热度数值
- 顶部提示"双击任意热点即可打开原文链接"
- 纯白背景 + 微灰边框，干净清爽
- Tab 按钮卡片风格，激活态实心彩色
- 列表增加表头行（# / 标题来源 / 条数）
- 窗口默认 1100×820

## v4.1 Bug修复 — 数据重复 + API配置
- **修复**：`fetch_rss()` 改为接受 `categories` 参数，体育/娱乐不再拿到相同数据
- **修复**：FetchWorker 分类传递：娱乐→RSS(entertainment)，体育→RSS(sports)，电影→TMDB
- **新增**：更多RSS源（Hollywood Reporter, Billboard, Yahoo Sports）
- **新增**：空面板显示API配置提示（TMDB/YouTube Key缺失时）
- 翻译：DeepSeek API测试通过，可以正常翻译

## v4.2 全API接入
- YouTube: AIzaSyAFsEN9EiBrn9mZzmh3Yl1fMIdBCZMv8FM ✅
- TMDB: 9f842807e0c4919d22ad8903bbd210a4 ✅  
- NewsAPI: 3bd17c719f174ce797556f31ffe01f64 ✅
- DeepSeek: sk-2b1544c282cd44709b2408745f77726c ✅
- 全部4个API测试通过，6个分类数据源全部打通

## v5 色调柔和 + 批量翻译
- **全部翻译按钮**：每个分类顶部新增"全部翻译"，一键翻译该分类所有标题，显示进度 X/20
- **色调调整**：6个分类颜色全面调柔，不再刺眼
- **Tab按钮**：从实心彩色 → 透明底+下划线样式，与白色主题完美融合
- **翻译按钮**：从实心彩色hover → 透明底+淡色边框，风格统一
- **刷新按钮**：保持蓝色主调，适配整体风格

## v5.1 布局优化 — 翻译按钮前置 + 来源标签统一
- **"译"按钮**：从最右侧移到序号后面（`02 [译] ▌标题`），窗口再小也永远可见
- **移除↗链接按钮**：双击整行已能打开原文，不再需要单独的链接按钮
- **来源标签**：从分类色字+同色浅底 → 统一灰字 `#475569` + 浅灰底 `#F1F5F9` + 灰边框，清晰干净

## v6 新增两个分类 + 修复水平滚动
- **🐦 社交热点**：TrendMCP `google news` 实时热门新闻（X 需要付费 $100/月，用 Google News 替代）
- **🔍 搜索趋势**：TrendMCP `Google Trends` 热门搜索词
- **TrendMCP API Key**：`tmcp_live_vj4mz9ctf815ne95yjnuwhstups8eh0c` 写入 .env
- **修复**：`scroll.setHorizontalScrollBarPolicy(AlwaysOff)` 禁用水平滚动条，"全部翻译"按钮不再被挤出视野
- 分类从 6 个 → 8 个，Tab 自适应布局

## v6.1 X API 按照官方文档接入
- `fetch_x_trends()` 按 TrendMCP 官方文档格式：`type: "X (Twitter)"`，返回 `[rank, name]` 对
- 免费套餐 X 数据返回 0 → 自动降级到 Google News 兜底
- 升级付费套餐后 X 数据会自动生效

## v7 Web版 — Flask 全平台迁移
- **app.py**：Flask 后端，复用所有数据获取逻辑，提供 `/api/refresh`、`/api/translate`、`/api/translate-batch`
- **templates/index.html**：纯 HTML/CSS/JS 前端，浅色主题，8 个 Tab，平移 PyQt 全部功能
- 启动：`python app.py` → 浏览器打开 `http://localhost:5000`
- Windows / Mac / Linux 全平台通用，同事 Safari/Chrome 直接打开
- 批量翻译、双击跳转、全部翻译 功能完整迁移
- 原 PyQt 版 main.py 保留不动
